import { Star } from 'lucide-react'
interface RatingStarsProps { value: number; max?: number; onChange?: (value: number) => void; label?: string }
export function RatingStars({ value, max = 5, onChange, label = 'Rating' }: RatingStarsProps) { return <div role={onChange ? 'radiogroup' : undefined} aria-label={label} className="flex items-center gap-0.5">{Array.from({ length: max }, (_, index) => { const rating = index + 1; const star = <Star size={18} fill={rating <= value ? 'currentColor' : 'none'} />; return onChange ? <button key={rating} type="button" role="radio" aria-checked={rating === value} aria-label={`${rating} out of ${max} stars`} onClick={() => onChange(rating)} className="rounded p-0.5 text-amber-500 hover:bg-amber-50 focus:outline-none focus:ring-2 focus:ring-amber-400">{star}</button> : <span key={rating} className="text-amber-500" aria-hidden="true">{star}</span> })}</div> }