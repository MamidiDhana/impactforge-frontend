import type { InputHTMLAttributes } from 'react'
interface FormFieldProps extends InputHTMLAttributes<HTMLInputElement> { label: string; error?: string; hint?: string }
export function FormField({ label, error, hint, id, ...props }: FormFieldProps) { const fieldId = id ?? props.name ?? label.toLowerCase().replace(/\s+/g, '-'); return <div className="space-y-1.5"><label htmlFor={fieldId} className="text-sm font-semibold text-slate-700">{label}{props.required && <span className="ml-1 text-red-600">*</span>}</label><input {...props} id={fieldId} aria-invalid={Boolean(error)} aria-describedby={error ? `${fieldId}-error` : hint ? `${fieldId}-hint` : undefined} className={`w-full rounded-lg border bg-white px-3.5 py-2.5 text-sm text-slate-800 outline-none transition placeholder:text-slate-400 focus:border-[#187e8d] focus:ring-2 focus:ring-[#187e8d]/20 ${error ? 'border-red-400' : 'border-slate-200'}`} />{hint && !error && <p id={`${fieldId}-hint`} className="text-xs text-slate-500">{hint}</p>}{error && <p id={`${fieldId}-error`} className="text-xs text-red-600">{error}</p>}</div> }