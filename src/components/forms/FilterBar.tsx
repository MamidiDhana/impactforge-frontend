import { RotateCcw } from 'lucide-react'
import { SearchInput } from './SearchInput'
interface FilterOption { label: string; value: string }
interface Filter { id: string; label: string; value: string; options: FilterOption[]; onChange: (value: string) => void }
interface FilterBarProps { searchValue?: string; onSearchChange?: (value: string) => void; filters?: Filter[]; onReset?: () => void; searchPlaceholder?: string }
export function FilterBar({ searchValue = '', onSearchChange, filters = [], onReset, searchPlaceholder }: FilterBarProps) { return <div className="flex flex-col gap-3 rounded-xl border border-slate-200 bg-white p-3 sm:flex-row sm:flex-wrap sm:items-end">{onSearchChange && <div className="min-w-56 flex-1"><SearchInput value={searchValue} onChange={onSearchChange} placeholder={searchPlaceholder} /></div>}{filters.map((filter) => <label key={filter.id} className="min-w-40 flex-1 text-xs font-semibold text-slate-600">{filter.label}<select value={filter.value} onChange={(event) => filter.onChange(event.target.value)} className="mt-1.5 w-full rounded-lg border border-slate-200 bg-white px-3 py-2.5 text-sm font-normal text-slate-700 outline-none focus:border-[#187e8d] focus:ring-2 focus:ring-[#187e8d]/20">{filter.options.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select></label>)}{onReset && <button type="button" onClick={onReset} className="inline-flex items-center justify-center gap-2 rounded-lg px-3 py-2.5 text-sm font-semibold text-slate-600 hover:bg-slate-100"><RotateCcw size={15} />Reset</button>}</div> }