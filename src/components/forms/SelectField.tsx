import type { SelectHTMLAttributes } from 'react'
interface SelectOption { label: string; value: string }
interface SelectFieldProps extends SelectHTMLAttributes<HTMLSelectElement> { label: string; options: SelectOption[]; error?: string }
export function SelectField({ label, options, error, id, ...props }: SelectFieldProps) { const fieldId = id ?? props.name ?? label.toLowerCase().replace(/\s+/g, '-'); return <div className="space-y-1.5"><label htmlFor={fieldId} className="text-sm font-semibold text-slate-700">{label}{props.required && <span className="ml-1 text-red-600">*</span>}</label><select {...props} id={fieldId} aria-invalid={Boolean(error)} className={`w-full rounded-lg border bg-white px-3.5 py-2.5 text-sm text-slate-800 outline-none focus:border-[#187e8d] focus:ring-2 focus:ring-[#187e8d]/20 ${error ? 'border-red-400' : 'border-slate-200'}`}>{options.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select>{error && <p className="text-xs text-red-600">{error}</p>}</div> }