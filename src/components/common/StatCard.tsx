import type { LucideIcon } from 'lucide-react'
import { ArrowDownRight, ArrowUpRight } from 'lucide-react'
import { cn } from '../../lib/utils'
interface StatCardProps { label: string; value: string | number; description?: string; icon?: LucideIcon; trend?: string; trendDirection?: 'up' | 'down' | 'neutral'; loading?: boolean }
export function StatCard({ label, value, description, icon: Icon, trend, trendDirection = 'up', loading = false }: StatCardProps) { if (loading) return <div className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="h-3 w-24 animate-pulse rounded bg-slate-200" /><div className="mt-4 h-8 w-20 animate-pulse rounded bg-slate-200" /><div className="mt-3 h-3 w-32 animate-pulse rounded bg-slate-100" /></div>; return <article className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-start justify-between gap-3"><div><p className="text-sm font-medium text-slate-500">{label}</p><p className="mt-2 font-[Manrope] text-2xl font-bold text-[#13243b]">{value}</p></div>{Icon && <span className="grid size-10 place-items-center rounded-lg bg-[#e8f5f5] text-[#187e8d]"><Icon size={19} /></span>}</div>{(description || trend) && <div className="mt-4 flex flex-wrap items-center gap-2 text-xs text-slate-500">{trend && <span className={cn('inline-flex items-center gap-0.5 font-semibold', trendDirection === 'down' ? 'text-red-600' : trendDirection === 'up' ? 'text-emerald-600' : 'text-slate-500')}>{trendDirection === 'down' ? <ArrowDownRight size={14} /> : trendDirection === 'up' ? <ArrowUpRight size={14} /> : null}{trend}</span>}{description && <span>{description}</span>}</div>}</article> }