import { AlertCircle } from 'lucide-react'
interface ErrorStateProps { title?: string; description?: string; onRetry?: () => void }
export function ErrorState({ title = 'Something went wrong', description = 'We could not load this information. Please try again.', onRetry }: ErrorStateProps) { return <div role="alert" className="flex flex-col items-center justify-center rounded-xl border border-red-200 bg-red-50 px-6 py-10 text-center"><AlertCircle className="text-red-600" size={28} /><h3 className="mt-3 font-[Manrope] font-bold text-red-900">{title}</h3><p className="mt-1 max-w-sm text-sm text-red-700">{description}</p>{onRetry && <button type="button" onClick={onRetry} className="mt-5 rounded-lg bg-red-700 px-4 py-2 text-sm font-semibold text-white hover:bg-red-800 focus:outline-none focus:ring-2 focus:ring-red-500 focus:ring-offset-2">Try again</button>}</div> }