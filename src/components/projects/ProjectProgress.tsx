interface ProjectProgressProps { percentage: number; currentStage: string; completedStage?: string; remainingStage?: string }
export function ProjectProgress({ percentage, currentStage, completedStage = 'Completed', remainingStage = 'Remaining' }: ProjectProgressProps) { const safePercentage = Math.min(100, Math.max(0, percentage)); return <div><div className="mb-2 flex items-center justify-between text-xs font-semibold"><span className="text-slate-600">{currentStage}</span><span className="text-[#187e8d]">{safePercentage}%</span></div><div className="h-2 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-[#1c91a1] transition-[width]" style={{ width: `${safePercentage}%` }} /></div><div className="mt-2 flex justify-between text-[11px] text-slate-400"><span>{completedStage}</span><span>{remainingStage}</span></div></div> }