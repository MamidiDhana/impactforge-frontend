import { useState } from 'react'
import { Plus, Users } from 'lucide-react'
import { HEILayout } from '../../layouts/HEILayout'
import { HEIPage } from './HEIShared'
import { Modal } from '../../components/common/Modal'
import { FormField } from '../../components/forms/FormField'
import { heiTeams } from '../../data/heiTeams'
export function TeamsPage() { const [open, setOpen] = useState(false); return <HEILayout title="Student Teams"><HEIPage title="Student teams" description="Form interdisciplinary teams around accepted community challenges." breadcrumbs={[{ label: 'University', href: '/hei/dashboard' }, { label: 'Student Teams' }]} action={<button type="button" onClick={() => setOpen(true)} className="inline-flex items-center gap-2 rounded-lg bg-[#12365a] px-4 py-2.5 text-sm font-bold text-white"><Plus size={16} />Create team</button>}><div className="grid gap-4 md:grid-cols-2">{heiTeams.map((team) => <article key={team.id} className="rounded-xl border border-slate-200 bg-white p-5"><div className="flex items-start justify-between gap-3"><div className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-lg bg-[#e8f5f5] text-[#187e8d]"><Users size={18} /></span><div><h2 className="font-[Manrope] font-bold text-[#13243b]">{team.name}</h2><p className="text-xs text-slate-500">{team.relatedProject}</p></div></div><span className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold text-slate-600">{team.status}</span></div><p className="mt-4 text-sm text-slate-500">Mentor: {team.facultyMentor} · {team.studentCount} students</p><div className="mt-3 flex flex-wrap gap-2">{team.requiredCapabilities.map((item) => <span key={item} className="rounded-full bg-slate-100 px-2.5 py-1 text-xs text-slate-600">{item}</span>)}</div><button type="button" className="mt-4 text-sm font-semibold text-[#187e8d]">View team</button></article>)}</div><Modal open={open} title="Create student team" onClose={() => setOpen(false)}><div className="grid gap-4"><FormField label="Team name" required /><FormField label="Related challenge or project" required /><FormField label="Faculty mentor" required /><FormField label="Team description" /><FormField label="Student members" placeholder="Search and select students" /><FormField label="Team capabilities" placeholder="Separate capabilities with commas" /><button type="button" onClick={() => setOpen(false)} className="rounded-lg bg-[#12365a] px-4 py-2.5 text-sm font-bold text-white">Create team</button></div></Modal></HEIPage></HEILayout> }