import { useState } from 'react'
import { Package, Plus } from 'lucide-react'
import { HEILayout } from '../../layouts/HEILayout'
import { HEIPage } from './HEIShared'
import { Modal } from '../../components/common/Modal'
import { FormField } from '../../components/forms/FormField'
import { heiResources } from '../../data/heiResources'
export function ResourcesPage() { const [open, setOpen] = useState(false); return <HEILayout title="Resources"><HEIPage title="Resources" description="Make laboratories, facilities, funding, and technical support visible." breadcrumbs={[{ label: 'University', href: '/hei/dashboard' }, { label: 'Resources' }]} action={<button type="button" onClick={() => setOpen(true)} className="inline-flex items-center gap-2 rounded-lg bg-[#12365a] px-4 py-2.5 text-sm font-bold text-white"><Plus size={16} />Add resource</button>}><div className="grid gap-4 md:grid-cols-2 lg:grid-cols-3">{heiResources.map((resource) => <article key={resource.id} className="rounded-xl border border-slate-200 bg-white p-5"><Package className="text-[#187e8d]" /><h2 className="mt-4 font-[Manrope] font-bold text-[#13243b]">{resource.name}</h2><p className="mt-1 text-xs text-[#187e8d]">{resource.type} · {resource.availability}</p><p className="mt-3 text-sm leading-6 text-slate-500">{resource.description}</p><p className="mt-4 text-xs text-slate-500">{resource.department} · {resource.currentUsage}</p><button type="button" className="mt-4 text-sm font-semibold text-[#187e8d]">View details</button></article>)}</div><Modal open={open} title="Add institutional resource" onClose={() => setOpen(false)}><div className="grid gap-4"><FormField label="Resource name" required /><FormField label="Resource type" required /><FormField label="Description" /><FormField label="Department" /><FormField label="Availability" /><FormField label="Contact person" /><button type="button" onClick={() => setOpen(false)} className="rounded-lg bg-[#12365a] px-4 py-2.5 text-sm font-bold text-white">Save resource</button></div></Modal></HEIPage></HEILayout> }