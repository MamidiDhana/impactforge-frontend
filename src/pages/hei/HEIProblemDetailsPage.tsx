import { ArrowRight, Bot, GraduationCap } from 'lucide-react'
import { Link, useParams } from 'react-router-dom'
import { HEILayout } from '../../layouts/HEILayout'
import { HEIPage } from './HEIShared'
import { ResponsiveCard } from '../../components/common/ResponsiveCard'
import { EmptyState } from '../../components/common/EmptyState'
import { heiProblems } from '../../data/heiProblems'
export function HEIProblemDetailsPage() { const { id } = useParams(); const problem = heiProblems.find((item) => item.id === id); if (!problem) return <HEILayout title="Problem details"><HEIPage title="Problem not found"><EmptyState title="Problem not found" action={<Link to="/hei/recommended-problems" className="rounded-lg bg-[#12365a] px-4 py-2 text-sm font-semibold text-white">Back to recommendations</Link>} /></HEIPage></HEILayout>; return <HEILayout title="Problem Details"><HEIPage title={problem.title} description="Review the validated challenge and assess your university's fit." breadcrumbs={[{ label: 'University', href: '/hei/dashboard' }, { label: 'Problems', href: '/hei/recommended-problems' }, { label: 'Problem Details' }]}><div className="grid gap-6 lg:grid-cols-[1.1fr_0.9fr]"><ResponsiveCard><p className="text-sm leading-7 text-slate-600">{problem.description}</p><div className="mt-5 grid gap-3 border-t border-slate-100 pt-5 text-sm text-slate-600 sm:grid-cols-2"><span>Category: <b>{problem.category}</b></span><span>Location: <b>{problem.location}</b></span><span>Affected people: <b>{problem.affectedPeople.toLocaleString()}</b></span><span>Urgency: <b>{problem.urgency}</b></span><span>Validated: <b>{problem.validationDate}</b></span></div><h2 className="mt-6 font-[Manrope] font-bold text-[#13243b]">Required capabilities</h2><div className="mt-3 flex flex-wrap gap-2">{problem.requiredCapabilities.map((item) => <span key={item} className="rounded-full bg-slate-100 px-3 py-1.5 text-xs text-slate-600">{item}</span>)}</div><p className="mt-5 text-sm leading-6 text-slate-500"><b>Existing efforts:</b> {problem.existingEfforts}<br /><b>Expected outcome:</b> {problem.expectedOutcome}</p></ResponsiveCard><ResponsiveCard className="border-[#b8dfe0]"><div className="flex items-center gap-3"><Bot className="text-[#187e8d]" /><h2 className="font-[Manrope] font-bold text-[#13243b]">Mock AI-assisted capability analysis</h2></div><p className="mt-2 text-xs text-slate-500">Illustrative analysis only.</p><div className="mt-5 space-y-4">{problem.requiredCapabilities.map((capability, index) => <div key={capability} className="rounded-lg bg-slate-50 p-3"><p className="text-sm font-semibold text-slate-700">{capability}</p><p className="mt-1 text-xs text-slate-500">Importance: {index === 0 ? 'Critical' : 'High'} · Available internally: {index === 1 ? 'Partial' : 'Yes'} · Gap: {index === 1 ? 'Needs review' : 'Low'}</p><p className="mt-2 text-xs font-semibold text-[#187e8d]">Suggested action: Assign faculty or use an institutional resource</p></div>)}</div></ResponsiveCard></div><ResponsiveCard className="mt-6"><h2 className="font-[Manrope] font-bold text-[#13243b]">Suggested direction</h2><p className="mt-2 text-sm text-slate-600">{problem.suggestedProjectType}. Faculty expertise: {problem.facultyExpertise.join(' · ')}.</p><p className="mt-2 text-sm text-slate-600">Resources: {problem.suggestedResources.join(' · ')}. Partner types: {problem.suggestedPartnerTypes.join(' · ')}.</p></ResponsiveCard><div className="mt-6 flex flex-wrap gap-3"><Link to="/hei/recommended-problems" className="rounded-lg bg-[#12365a] px-4 py-2.5 text-sm font-bold text-white">Accept challenge</Link><button type="button" className="rounded-lg border border-slate-200 px-4 py-2.5 text-sm font-semibold text-slate-700">Save for later</button><Link to="/hei/capability-gaps" className="inline-flex items-center gap-2 rounded-lg border border-slate-200 px-4 py-2.5 text-sm font-semibold text-slate-700">View similar problems <ArrowRight size={15} /></Link><button type="button" className="rounded-lg border border-slate-200 px-4 py-2.5 text-sm font-semibold text-slate-700"><GraduationCap className="mr-1 inline" size={15} />Contact validator</button></div></HEIPage></HEILayout> }