import { Bot, CheckCircle2 } from 'lucide-react'
import { HEILayout } from '../../layouts/HEILayout'
import { HEIPage } from './HEIShared'
import { heiCapabilityGaps } from '../../data/heiCapabilityGaps'
export function CapabilityGapsPage() { return <HEILayout title="Progress"><HEIPage title="Progress" description="Identify missing expertise and resources before committing to delivery." breadcrumbs={[{ label: 'University', href: '/hei/dashboard' }, { label: 'Progress' }]}><div className="mb-5 flex gap-3 rounded-xl border border-[#b8dfe0] bg-[#e8f5f5] p-4 text-sm text-slate-600"><Bot className="shrink-0 text-[#187e8d]" /><span><b>Mock AI-assisted capability-gap analysis</b><br />These suggestions are illustrative and require university review.</span></div><div className="grid gap-4 lg:grid-cols-2">{heiCapabilityGaps.map((gap) => <article key={gap.id} className="rounded-xl border border-slate-200 bg-white p-5"><div className="flex items-start justify-between gap-3"><h2 className="font-[Manrope] font-bold text-[#13243b]">{gap.subject}</h2><span className={`rounded-full px-2.5 py-1 text-xs font-bold ${gap.severity === 'High' ? 'bg-red-50 text-red-700' : 'bg-amber-50 text-amber-700'}`}>{gap.severity} gap</span></div><div className="mt-4 grid gap-3 text-sm text-slate-600"><p>Required: <b>{gap.required}</b></p><p>Available internally: <b>{gap.availableInternally}</b></p><p>Missing: <b>{gap.missing}</b></p><p>Suggested solution: {gap.suggestedSolution}</p><p>Partner type: {gap.partnerType}</p></div><button type="button" onClick={() => undefined} className="mt-5 inline-flex items-center gap-2 rounded-lg bg-[#12365a] px-3 py-2 text-xs font-semibold text-white"><CheckCircle2 size={14} />{gap.action}</button></article>)}</div></HEIPage></HEILayout> }