import { useState } from 'react'
import { GraduationCap, Plus } from 'lucide-react'
import { HEILayout } from '../../layouts/HEILayout'
import { HEIPage } from './HEIShared'
import { SearchInput } from '../../components/forms/SearchInput'
import { FormField } from '../../components/forms/FormField'
import { Modal } from '../../components/common/Modal'
import { heiFaculty } from '../../data/heiFaculty'
export function FacultyPage() { const [search, setSearch] = useState(''); const [open, setOpen] = useState(false); const results = heiFaculty.filter((item) => `${item.name} ${item.department} ${item.expertise.join(' ')}`.toLowerCase().includes(search.toLowerCase())); return <HEILayout title="Faculty"><HEIPage title="Faculty" description="Manage institutional experts contributing to community challenges." breadcrumbs={[{ label: 'University', href: '/hei/dashboard' }, { label: 'Faculty' }]} action={<button type="button" onClick={() => setOpen(true)} className="inline-flex items-center gap-2 rounded-lg bg-[#12365a] px-4 py-2.5 text-sm font-bold text-white"><Plus size={16} />Add faculty</button>}><div className="max-w-md"><SearchInput value={search} onChange={setSearch} placeholder="Search faculty or expertise" /></div><div className="mt-5 grid gap-4 md:grid-cols-2">{results.map((member) => <article key={member.id} className="rounded-xl border border-slate-200 bg-white p-5"><div className="flex items-start gap-3"><span className="grid size-10 place-items-center rounded-full bg-[#d9eeee] text-[#12365a]"><GraduationCap size={18} /></span><div><h2 className="font-[Manrope] font-bold text-[#13243b]">{member.name}</h2><p className="text-xs text-slate-500">{member.designation} · {member.department}</p></div></div><div className="mt-4 flex flex-wrap gap-2">{member.expertise.map((item) => <span key={item} className="rounded-full bg-slate-100 px-2.5 py-1 text-xs text-slate-600">{item}</span>)}</div><p className="mt-4 text-sm text-slate-500">{member.experience} years · {member.availability} · {member.currentProjects} projects</p><div className="mt-4 flex gap-3"><button type="button" className="text-sm font-semibold text-[#187e8d]">View profile</button><button type="button" className="text-sm font-semibold text-[#12365a]">Assign to challenge</button></div></article>)}</div><Modal open={open} title="Add faculty member" onClose={() => setOpen(false)}><div className="grid gap-4"><FormField label="Full name" required /><FormField label="Department" required /><FormField label="Designation" /><FormField label="Email" type="email" /><FormField label="Areas of expertise" placeholder="Separate areas with commas" /><FormField label="Years of experience" type="number" /><button type="button" onClick={() => setOpen(false)} className="rounded-lg bg-[#12365a] px-4 py-2.5 text-sm font-bold text-white">Save faculty</button></div></Modal></HEIPage></HEILayout> }