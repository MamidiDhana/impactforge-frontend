import { useState } from 'react'
import { Building2, CheckCircle2, LockKeyhole } from 'lucide-react'
import { HEILayout } from '../../layouts/HEILayout'
import { HEIPage } from './HEIShared'
import { FormField } from '../../components/forms/FormField'
export function HEIProfilePage() { const [editing, setEditing] = useState(false); const [saved, setSaved] = useState(false); return <HEILayout title="Profile"><HEIPage title="Profile" description="Manage the institutional information shown to potential collaborators." breadcrumbs={[{ label: 'University', href: '/hei/dashboard' }, { label: 'Profile' }]} action={<button type="button" onClick={() => setEditing(!editing)} className="rounded-lg bg-[#12365a] px-4 py-2.5 text-sm font-bold text-white">{editing ? 'Cancel editing' : 'Edit profile'}</button>}><form onSubmit={(event) => { event.preventDefault(); setSaved(true); setEditing(false) }} className="max-w-3xl rounded-xl border border-slate-200 bg-white p-6 shadow-sm sm:p-8"><div className="flex items-center gap-4 border-b border-slate-100 pb-6"><span className="grid size-14 place-items-center rounded-lg bg-[#d9eeee] text-[#12365a]"><Building2 size={25} /></span><div><h2 className="font-[Manrope] text-xl font-bold text-[#13243b]">National Institute of Technology Karnataka</h2><p className="text-sm text-slate-500">Public technical university · Surathkal, Karnataka</p></div><span className="ml-auto hidden items-center gap-1 text-xs font-semibold text-emerald-700 sm:flex"><CheckCircle2 size={15} />Verified</span></div><div className="mt-6 grid gap-5 md:grid-cols-2"><FormField label="University name" defaultValue="National Institute of Technology Karnataka" disabled={!editing} /><FormField label="University type" defaultValue="Public technical university" disabled={!editing} /><FormField label="Location" defaultValue="Surathkal, Karnataka" disabled={!editing} /><FormField label="Website" defaultValue="https://example.edu" disabled={!editing} /><FormField label="Contact email" defaultValue="collaborate@example.edu" disabled={!editing} /><FormField label="Contact phone" placeholder="Office phone" disabled={!editing} /><FormField label="Departments" defaultValue="Engineering, Computer Science, Social Sciences" disabled={!editing} /><FormField label="Areas of expertise" defaultValue="Water systems, Data science, Service design" disabled={!editing} /></div><div className="mt-5"><FormField label="Available resources" defaultValue="Water quality lab, Field sensors, Innovation fund" disabled={!editing} /></div>{saved && <p role="status" className="mt-5 flex items-center gap-2 rounded-lg bg-emerald-50 p-3 text-sm text-emerald-800"><CheckCircle2 size={17} />Profile changes saved to this mock session.</p>}<div className="mt-6 flex gap-3"><button type="submit" disabled={!editing} className="rounded-lg bg-[#12365a] px-4 py-2.5 text-sm font-bold text-white disabled:opacity-50">Save changes</button><button type="button" className="inline-flex items-center gap-2 rounded-lg border border-slate-200 px-4 py-2.5 text-sm font-semibold text-slate-700"><LockKeyhole size={16} />Change password</button></div></form></HEIPage></HEILayout> }