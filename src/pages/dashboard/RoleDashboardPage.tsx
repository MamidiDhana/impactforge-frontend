import { useNavigate } from 'react-router-dom'
import { DashboardTopbar } from '../../components/navigation/DashboardTopbar'
import { PageContainer } from '../../components/common/PageContainer'
import { useAuth } from '../../context/AuthContext'
import { ROLE_DASHBOARD_PATH } from '../../routes/rolePaths'
import type { UserRole } from '../../types'
const labels: Record<UserRole, string> = { citizen: 'Citizen', government: 'Government Validator', hei: 'University / HEI', faculty: 'Faculty', partner: 'Partner', admin: 'Administrator' }
export function RoleDashboardPage() { const { currentUser, logout } = useAuth(); const navigate = useNavigate(); if (!currentUser) return null; const signOut = () => { logout(); navigate('/login', { replace: true }) }; return <div className="min-h-screen bg-[#f7f9fc]"><DashboardTopbar title={`${labels[currentUser.role]} workspace`} user={currentUser} onProfile={() => navigate('/profile')} onLogout={signOut} /><PageContainer><div className="rounded-xl border border-slate-200 bg-white p-6 shadow-sm sm:p-8"><p className="text-sm font-bold uppercase tracking-[0.12em] text-[#187e8d]">Mock dashboard placeholder</p><h1 className="mt-3 font-[Manrope] text-3xl font-bold text-[#13243b]">Welcome, {currentUser.name}</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-500">The {labels[currentUser.role].toLowerCase()} dashboard will be built in the next phase. Your frontend session is active for route and navigation testing.</p><div className="mt-6 flex flex-wrap gap-3"><button type="button" onClick={signOut} className="rounded-lg bg-[#12365a] px-4 py-2.5 text-sm font-bold text-white">Log out</button><button type="button" onClick={() => navigate('/')} className="rounded-lg border border-slate-200 px-4 py-2.5 text-sm font-bold text-slate-700">Back to home</button></div></div><p className="mt-4 text-xs text-slate-400">Signed in as {currentUser.email} · {ROLE_DASHBOARD_PATH[currentUser.role]}</p></PageContainer></div> }