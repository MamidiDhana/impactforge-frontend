import { Search } from 'lucide-react'
import { Link } from 'react-router-dom'
import { PageContainer } from '../../components/common/PageContainer'
import { PageHeader } from '../../components/common/PageHeader'
import { ProjectProgress } from '../../components/projects/ProjectProgress'
import { EmptyState } from '../../components/common/EmptyState'
import { workspaceProjects } from '../../data/workspaceProjects'
export function ProjectsPage() { return <PageContainer><PageHeader title="Project Workspace" description="Collaborate across validated problems, teams, universities, and partners." breadcrumbs={[{ label: 'Projects' }]} /><div className="grid gap-4 md:grid-cols-2">{workspaceProjects.map((project) => <article key={project.id} className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-[0.1em] text-[#187e8d]">{project.category}</p><h2 className="mt-2 font-[Manrope] text-xl font-bold text-[#13243b]">{project.name}</h2></div><span className="rounded-full bg-emerald-50 px-2.5 py-1 text-xs font-semibold text-emerald-700">{project.health}</span></div><p className="mt-3 text-sm text-slate-500">{project.problemTitle} · {project.location}</p><p className="mt-2 text-sm text-slate-600">{project.university} · {project.facultyLead}</p><div className="mt-5"><ProjectProgress percentage={project.progress} currentStage={project.stage} /></div><Link to={`/projects/${project.id}/overview`} className="mt-5 inline-flex rounded-lg bg-[#12365a] px-4 py-2.5 text-sm font-bold text-white">Open workspace</Link></article>)}</div>{workspaceProjects.length === 0 && <EmptyState icon={Search} title="No projects available" description="Projects connected to your work will appear here." />}</PageContainer> }