import { useState } from 'react'
import { CheckCircle2, LockKeyhole, UserRound } from 'lucide-react'
import { GovernmentLayout } from '../../layouts/GovernmentLayout'
import { GovPage } from './GovernmentShared'
import { FormField } from '../../components/forms/FormField'
import { useAuth } from '../../context/AuthContext'
export function GovernmentProfilePage() { const { currentUser } = useAuth(); const [editing, setEditing] = useState(false); const [saved, setSaved] = useState(false); if (!currentUser) return null; return <GovernmentLayout title="Profile"><GovPage title="Profile" description="Manage your mock profile information." breadcrumbs={[{ label: 'Government', href: '/government/dashboard' }, { label: 'Profile' }]} action={<button type="button" onClick={() => setEditing(!editing)} className="rounded-lg bg-[#12365a] px-4 py-2.5 text-sm font-bold text-white">{editing ? 'Cancel editing' : 'Edit profile'}</button>}><form onSubmit={(event) => { event.preventDefault(); setSaved(true); setEditing(false) }} className="max-w-3xl rounded-xl border border-slate-200 bg-white p-6 shadow-sm sm:p-8"><div className="flex items-center gap-4 border-b border-slate-100 pb-6"><span className="grid size-14 place-items-center rounded-full bg-[#d9eeee] text-[#12365a]"><UserRound size={24} /></span><div><h2 className="font-[Manrope] text-xl font-bold text-[#13243b]">{currentUser.name}</h2><p className="text-sm text-slate-500">District Innovation Cell · Government Validator</p></div></div><div className="mt-6 grid gap-5 md:grid-cols-2"><FormField label="Officer name" defaultValue={currentUser.name} disabled={!editing} /><FormField label="Department" defaultValue="District Innovation Cell" disabled={!editing} /><FormField label="Designation" defaultValue="Government Validator" disabled={!editing} /><FormField label="Email" defaultValue={currentUser.email} disabled /><FormField label="Phone number" placeholder="Office phone" disabled={!editing} /><FormField label="Office location" defaultValue="Kolar, Karnataka" disabled={!editing} /></div>{saved && <p role="status" className="mt-5 flex items-center gap-2 rounded-lg bg-emerald-50 p-3 text-sm text-emerald-800"><CheckCircle2 size={17} />Profile changes saved to this mock session.</p>}<div className="mt-6 flex gap-3"><button type="submit" disabled={!editing} className="rounded-lg bg-[#12365a] px-4 py-2.5 text-sm font-bold text-white disabled:opacity-50">Save changes</button><button type="button" className="inline-flex items-center gap-2 rounded-lg border border-slate-200 px-4 py-2.5 text-sm font-semibold text-slate-700"><LockKeyhole size={16} />Change password</button></div></form></GovPage></GovernmentLayout> }