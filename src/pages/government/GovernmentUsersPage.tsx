import { Search, UserRound } from 'lucide-react'
import { useState } from 'react'
import { GovernmentLayout } from '../../layouts/GovernmentLayout'
import { GovPage } from './GovernmentShared'
import { SearchInput } from '../../components/forms/SearchInput'
import { EmptyState } from '../../components/common/EmptyState'
import { governmentUsers } from '../../data/governmentUsers'
export function GovernmentUsersPage() { const [search, setSearch] = useState(''); const users = governmentUsers.filter((item) => `${item.name} ${item.organization} ${item.role}`.toLowerCase().includes(search.toLowerCase())); return <GovernmentLayout title="Users"><GovPage title="Users" description="Review participants relevant to government operations without exposing sensitive information." breadcrumbs={[{ label: 'Government', href: '/government/dashboard' }, { label: 'Users' }]}><div className="max-w-md"><SearchInput value={search} onChange={setSearch} placeholder="Search users or organizations" /></div>{users.length ? <div className="mt-5 grid gap-4 md:grid-cols-2">{users.map((user) => <article key={user.id} className="rounded-xl border border-slate-200 bg-white p-5"><div className="flex items-start gap-3"><span className="grid size-10 place-items-center rounded-full bg-[#d9eeee] text-[#12365a]"><UserRound size={18} /></span><div><h2 className="font-semibold text-[#13243b]">{user.name}</h2><p className="text-xs text-slate-500">{user.role} · {user.organization}</p></div></div><div className="mt-4 flex flex-wrap gap-2 text-xs"><span className="rounded-full bg-emerald-50 px-2.5 py-1 text-emerald-700">{user.verification}</span><span className="rounded-full bg-slate-100 px-2.5 py-1 text-slate-600">{user.accountStatus}</span><span className="text-slate-400">Last active {user.lastActive}</span></div><button type="button" className="mt-4 text-sm font-semibold text-[#187e8d]">View details</button></article>)}</div> : <div className="mt-5"><EmptyState icon={Search} title="No users found" /></div>}</GovPage></GovernmentLayout> }