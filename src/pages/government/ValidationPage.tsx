import { ClipboardCheck, ShieldCheck, XCircle } from 'lucide-react'
import { Link } from 'react-router-dom'
import { GovernmentLayout } from '../../layouts/GovernmentLayout'
import { GovPage } from './GovernmentShared'
import { StatCard } from '../../components/common/StatCard'
import { SectionHeader } from '../../components/common/SectionHeader'
import { CitizenStatusBadge } from '../../components/citizen/CitizenStatusBadge'
import { governmentProblems } from '../../data/governmentProblems'
export function ValidationPage() { return <GovernmentLayout title="Validation"><GovPage title="Validation" description="Review decisions determining whether a community problem can move forward." breadcrumbs={[{ label: 'Government', href: '/government/dashboard' }, { label: 'Validation' }]}><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><StatCard label="Pending" value="12" icon={ClipboardCheck} description="Demo data" /><StatCard label="In review" value="5" icon={ClipboardCheck} description="Demo data" /><StatCard label="Validated" value="24" icon={ShieldCheck} description="Demo data" /><StatCard label="Rejected" value="4" icon={XCircle} description="Demo data" /></div><section className="mt-8"><SectionHeader title="Problems awaiting review" /><div className="grid gap-3">{governmentProblems.filter((item) => item.status === 'Submitted' || item.status === 'Under Review').map((problem) => <div key={problem.id} className="flex flex-col gap-3 rounded-xl border border-slate-200 bg-white p-4 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="font-semibold text-[#13243b]">{problem.title}</h2><p className="mt-1 text-xs text-slate-500">{problem.category} · {problem.priority} priority</p></div><div className="flex items-center gap-3"><CitizenStatusBadge status={problem.status} /><Link to={`/government/problems/${problem.id}/review`} className="rounded-lg bg-[#12365a] px-3 py-2 text-xs font-semibold text-white">Review</Link></div></div>)}</div></section><section className="mt-8"><SectionHeader title="Recently validated" /><div className="grid gap-3">{governmentProblems.filter((item) => item.status === 'Validated' || item.status === 'Converted to Project').map((problem) => <div key={problem.id} className="rounded-xl border border-slate-200 bg-white p-4"><div className="flex items-center justify-between gap-3"><h2 className="font-semibold text-[#13243b]">{problem.title}</h2><CitizenStatusBadge status={problem.status} /></div><p className="mt-2 text-sm text-slate-500">Validation date: {problem.validationDate}</p></div>)}</div></section></GovPage></GovernmentLayout> }